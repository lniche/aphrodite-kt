spring:
  datasource:
    url: jdbc:postgresql://db:5432/test
    username: postgres
    password: 123123
  data:
    redis:
      database: 0
      host: cache
      port: 6379
      # password:
      timeout: 10s

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
